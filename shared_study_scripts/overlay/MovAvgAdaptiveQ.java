package com.motivewave.platform.study.overlay;import com.motivewave.platform.sdk.common.DataContext;import com.motivewave.platform.sdk.common.Defaults;import com.motivewave.platform.sdk.common.Enums;import com.motivewave.platform.sdk.common.Inputs;import com.motivewave.platform.sdk.common.desc.IndicatorDescriptor;import com.motivewave.platform.sdk.common.desc.InputDescriptor;import com.motivewave.platform.sdk.common.desc.IntegerDescriptor;import com.motivewave.platform.sdk.common.desc.PathDescriptor;import com.motivewave.platform.sdk.common.desc.SliderDescriptor;import com.motivewave.platform.sdk.common.desc.ValueDescriptor;import com.motivewave.platform.sdk.study.Study;import com.motivewave.platform.sdk.study.StudyHeader;/** Moving Average Adaptive Q 070 */@StudyHeader(  namespace="com.motivewave",  id="ID_MAAQ",  rb="com.motivewave.platform.study.nls.strings2",  name="NAME_MOVING_AVERAGE_ADAPTIVE_Q",  label="LBL_MAAQ",  desc="DESC_MAAQ",  helpLink="http://www.motivewave.com/studies/moving_average_adaptive_q.htm",  requiresVolume=false,  signals=false,  overlay=true,  studyOverlay=true)public class MovAvgAdaptiveQ extends Study{  enum Values { DIFF, MAAQ };  @Override  public void initialize(Defaults defaults)  {    var sd=createSD();    var tab=sd.addTab(get("TAB_GENERAL"));    var inputs=tab.addGroup(get("INPUTS"));    inputs.addRow(new InputDescriptor(Inputs.INPUT, get("INPUT"), Enums.BarInput.CLOSE));    inputs.addRow(new IntegerDescriptor(Inputs.PERIOD, get("PERIOD"), 10, 1, 999, 1));    var settings=tab.addGroup(get("PATH_INDICATOR"));    settings.addRow(new PathDescriptor(Inputs.PATH, get("LBL_MAAQ"), defaults.getLineColor(), 1.0f, null, true, true, true));    settings.addRow(new IndicatorDescriptor(Inputs.IND, get("IND"), null, null, false, true, true));    // Quick Settings (Tool Bar and Popup Editor)    sd.addQuickSettings(Inputs.INPUT);    sd.addQuickSettings(new SliderDescriptor(Inputs.PERIOD, get("PERIOD"), 10, 1, 9999, true, () -> Enums.Icon.SINE_WAVE.get()));    sd.addQuickSettings(Inputs.PATH);    var desc=createRD();    desc.exportValue(new ValueDescriptor(Values.MAAQ, get("LBL_MAAQ"), new String[] { Inputs.INPUT, Inputs.PERIOD }));    desc.setLabelSettings(Inputs.INPUT, Inputs.PERIOD);    desc.declarePath(Values.MAAQ, Inputs.PATH);    desc.declareIndicator(Values.MAAQ, Inputs.IND);    desc.setRangeKeys(Values.MAAQ);  }  @Override  public void onLoad(Defaults defaults)  {    int p1=getSettings().getInteger(Inputs.PERIOD);    setMinBars(p1 * 2);  }  @Override  protected void calculate(int index, DataContext ctx)  {    int period=getSettings().getInteger(Inputs.PERIOD);    if (index < period) return;    Object input=getSettings().getInput(Inputs.INPUT, Enums.BarInput.CLOSE);    var series=ctx.getDataSeries();    double fastest=0.6022;    double slowest=0.0645;    double price=series.getDouble(index, input, 0);    double prevP=series.getDouble(index - 1, input, 0);    double diff=Math.abs(price - prevP);    series.setDouble(index, Values.DIFF, diff);    if (index < period * 2) return;    double priorP=series.getDouble(index - period, input, 0);    double priorDiff=Math.abs(price - priorP);    double sumPrev=series.sum(index, period, Values.DIFF);    double prevMaaq=series.getDouble(index - 1, Values.MAAQ, price); // returns price on first try.    double temp=((priorDiff / sumPrev) * fastest) + slowest;    double maaq=prevMaaq + Math.pow(temp, 2) * (price - prevMaaq);    series.setDouble(index, Values.MAAQ, maaq);    series.setComplete(index);  }}