package com.motivewave.platform.study.overlay;import com.motivewave.platform.sdk.common.DataContext;import com.motivewave.platform.sdk.common.Defaults;import com.motivewave.platform.sdk.common.Enums;import com.motivewave.platform.sdk.common.Inputs;import com.motivewave.platform.sdk.common.desc.IndicatorDescriptor;import com.motivewave.platform.sdk.common.desc.InputDescriptor;import com.motivewave.platform.sdk.common.desc.IntegerDescriptor;import com.motivewave.platform.sdk.common.desc.PathDescriptor;import com.motivewave.platform.sdk.common.desc.SliderDescriptor;import com.motivewave.platform.sdk.common.desc.ValueDescriptor;import com.motivewave.platform.sdk.study.Study;import com.motivewave.platform.sdk.study.StudyHeader;/** Ehlers Filter Distance Coefficient John F. Ehlers 139 */@StudyHeader(  namespace="com.motivewave",  id="ID_EHLFDC",  rb="com.motivewave.platform.study.nls.strings2",  label="LBL_EHLFDC",  name="NAME_EHLERS_FILTER_DISTANCE_COEFF",  desc="DESC_EHLFDC",  menu="MENU_JOHN_EHLERS",  helpLink="http://www.motivewave.com/studies/ehlers_filter_distance_coeff.htm",  signals=false,  overlay=true,  studyOverlay=true)public class EhlersFilterDistCoff extends Study{  enum Values { FILT }  @Override  public void initialize(Defaults defaults)  {    var sd=createSD();    var tab=sd.addTab(get("TAB_GENERAL"));    var inputs=tab.addGroup(get("INPUTS"));    inputs.addRow(new InputDescriptor(Inputs.INPUT, get("INPUT"), Enums.BarInput.CLOSE));    inputs.addRow(new IntegerDescriptor(Inputs.PERIOD, get("PERIOD"), 20, 1, 9999, 1));    var settings=tab.addGroup(get("PATH_INDICATOR"));    settings.addRow(new PathDescriptor(Inputs.PATH, get("LBL_EHLFDC"), defaults.getLineColor(), 1.0f, null, true, false, true));    settings.addRow(new IndicatorDescriptor(Inputs.IND, get("IND"), defaults.getLineColor(), null, false, true, true));    // Quick Settings (Tool Bar and Popup Editor)    sd.addQuickSettings(Inputs.INPUT);    sd.addQuickSettings(new SliderDescriptor(Inputs.PERIOD, get("PERIOD"), 20, 1, 9999, true, () -> Enums.Icon.SINE_WAVE.get()));    sd.addQuickSettings(Inputs.PATH);    var desc=createRD();    desc.setLabelSettings(Inputs.INPUT, Inputs.PERIOD);    desc.exportValue(new ValueDescriptor(Values.FILT, get("LBL_EHLFDC"), new String[] { Inputs.INPUT, Inputs.PERIOD }));    desc.declarePath(Values.FILT, Inputs.PATH);    desc.declareIndicator(Values.FILT, Inputs.IND);    desc.setRangeKeys(Values.FILT);  }  @Override  public void onLoad(Defaults defaults)  {    int p1=getSettings().getInteger(Inputs.PERIOD);    setMinBars(p1 * 2);  }  @Override  protected void calculate(int index, DataContext ctx)  {    int p1=getSettings().getInteger(Inputs.PERIOD);    if (index < p1 * 2) return;    Object key=getSettings().getInput(Inputs.INPUT, Enums.BarInput.CLOSE);    var series=ctx.getDataSeries();    double price=0, priorP=0, num=0, sumC=0, filt=0;    double[] coef=new double[p1];    double[] d2=new double[p1];    int count=0;    for (int i=index - (p1 - 1); i <= index; i++) {      d2[count]=0;      price=series.getDouble(i, key, 0);      for (int lb=1; lb < p1; lb++) {        priorP=series.getDouble(i - lb, key, 0);        d2[count]=d2[count] + (price - priorP) * (price - priorP);      }      coef[count]=d2[count];      count++;    }    count=0;    for (int i=index - (p1 - 1); i <= index; i++) {      price=series.getDouble(i, key, 0);      num=num + coef[count] * price;      sumC=sumC + coef[count];      count++;    }    if (sumC != 0) filt=num / sumC;    series.setDouble(index, Values.FILT, filt);    series.setComplete(index);  }}