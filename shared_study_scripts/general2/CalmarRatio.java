package com.motivewave.platform.study.general2;import com.motivewave.platform.sdk.common.DataContext;import com.motivewave.platform.sdk.common.Defaults;import com.motivewave.platform.sdk.common.Enums;import com.motivewave.platform.sdk.common.Inputs;import com.motivewave.platform.sdk.common.desc.GuideDescriptor;import com.motivewave.platform.sdk.common.desc.IndicatorDescriptor;import com.motivewave.platform.sdk.common.desc.InputDescriptor;import com.motivewave.platform.sdk.common.desc.IntegerDescriptor;import com.motivewave.platform.sdk.common.desc.PathDescriptor;import com.motivewave.platform.sdk.common.desc.ShadeDescriptor;import com.motivewave.platform.sdk.common.desc.SliderDescriptor;import com.motivewave.platform.sdk.common.desc.ValueDescriptor;import com.motivewave.platform.sdk.study.Study;import com.motivewave.platform.sdk.study.StudyHeader;/** Calmar Ratio 155 */@StudyHeader(  namespace="com.motivewave",  id="ID_CALMAR",  rb="com.motivewave.platform.study.nls.strings2",  label="LBL_CALMAR",  name="NAME_CALMAR_RATIO",  desc="DESC_CALMAR",  helpLink="http://www.motivewave.com/studies/calmar_ratio.htm",  signals=false,  overlay=false,  studyOverlay=true)public class CalmarRatio extends Study{  enum Values { DD, RET, CALMAR }  @Override  public void initialize(Defaults defaults)  {    var sd=createSD();    var tab=sd.addTab(get("TAB_GENERAL"));    var inputs=tab.addGroup(get("INPUTS"));    inputs.addRow(new InputDescriptor(Inputs.INPUT, get("INPUT"), Enums.BarInput.CLOSE));    inputs.addRow(new IntegerDescriptor(Inputs.PERIOD, get("PERIOD"), 30, 1, 9999, 1));    var settings=tab.addGroup(get("PATH_INDICATOR"));    settings.addRow(new PathDescriptor(Inputs.PATH, get("LBL_CALMAR"), defaults.getLineColor(), 1.0f, null, true, false, true));    settings.addRow(new IndicatorDescriptor(Inputs.IND, get("IND"), defaults.getLineColor(), null, false, true, true));    tab=sd.addTab(get("TAB_DISPLAY"));    var guides=tab.addGroup(get("GUIDE"));    var mg=new GuideDescriptor(Inputs.MIDDLE_GUIDE, get("MIDDLE_GUIDE"), 0, -9.01, 9.01, .01, true);    mg.setDash(new float[] { 3, 3 });    guides.addRow(mg);    var shade=tab.addGroup(get("SHADING"));    shade.addRow(new ShadeDescriptor(Inputs.TOP_FILL, get("TOP_FILL"), Inputs.MIDDLE_GUIDE, Inputs.PATH,        Enums.ShadeType.ABOVE, defaults.getTopFillColor(), true, true));    shade.addRow(new ShadeDescriptor(Inputs.BOTTOM_FILL, get("BOTTOM_FILL"), Inputs.MIDDLE_GUIDE, Inputs.PATH,        Enums.ShadeType.BELOW, defaults.getBottomFillColor(), true, true));    // Quick Settings (Tool Bar and Popup Editor)    sd.addQuickSettings(Inputs.INPUT);    sd.addQuickSettings(new SliderDescriptor(Inputs.PERIOD, get("PERIOD"), 30, 1, 9999, true, () -> Enums.Icon.SINE_WAVE.get()));    sd.addQuickSettings(Inputs.PATH, Inputs.TOP_FILL, Inputs.BOTTOM_FILL);    var desc=createRD();    desc.setLabelSettings(Inputs.INPUT, Inputs.PERIOD);    desc.exportValue(new ValueDescriptor(Values.CALMAR, get("LBL_CALMAR"), new String[] { Inputs.INPUT, Inputs.PERIOD }));    desc.declarePath(Values.CALMAR, Inputs.PATH);    desc.declareIndicator(Values.CALMAR, Inputs.IND);    desc.setRangeKeys(Values.CALMAR);  }  @Override  public void onLoad(Defaults defaults)  {    int p1=getSettings().getInteger(Inputs.PERIOD);    setMinBars(p1 * 2);  }  @Override  protected void calculate(int index, DataContext ctx)  {    int p1=getSettings().getInteger(Inputs.PERIOD);    if (index < p1) return;    Object key=getSettings().getInput(Inputs.INPUT, Enums.BarInput.CLOSE);    var series=ctx.getDataSeries();    int barMin=0;    var bar=series.getBarSize();    if (bar.getType() == Enums.BarSizeType.LINEAR) barMin=bar.getIntervalMinutes();    else return;    double minPerYr=60 * 24 * 30 * 12;    double barsPerYr=minPerYr / barMin;    double price=series.getDouble(index, key, 0);    double priorP=series.getDouble(index - p1, key, 0);    double maxDn=series.highest(index, p1, key);    double dd=(price - maxDn) / maxDn;    series.setDouble(index, Values.DD, dd);    if (index < p1 * 2) return;    double ret=(price / priorP) - 1; // returns for the period    double power=barsPerYr / p1;    double anualRet=Math.pow((1 + ret), power) - 1; // convert to annual return    double maxDd=series.lowest(index, p1, Values.DD); // largest negative value    double calmar=anualRet / Math.abs(maxDd);    series.setDouble(index, Values.CALMAR, calmar);    series.setComplete(index);  }}