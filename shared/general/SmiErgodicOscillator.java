package com.motivewave.platform.study.general;import java.awt.Color;import com.motivewave.platform.sdk.common.DataContext;import com.motivewave.platform.sdk.common.Defaults;import com.motivewave.platform.sdk.common.Enums;import com.motivewave.platform.sdk.common.Inputs;import com.motivewave.platform.sdk.common.desc.ColorDescriptor;import com.motivewave.platform.sdk.common.desc.GuideDescriptor;import com.motivewave.platform.sdk.common.desc.IndicatorDescriptor;import com.motivewave.platform.sdk.common.desc.InputDescriptor;import com.motivewave.platform.sdk.common.desc.IntegerDescriptor;import com.motivewave.platform.sdk.common.desc.MAMethodDescriptor;import com.motivewave.platform.sdk.common.desc.SliderDescriptor;import com.motivewave.platform.sdk.common.desc.ValueDescriptor;import com.motivewave.platform.sdk.study.Study;import com.motivewave.platform.sdk.study.StudyHeader;/** SMI Ergodic Oscillator 011 */@StudyHeader(  namespace="com.motivewave",  id="ID_SMIEO",  rb="com.motivewave.platform.study.nls.strings2",  name="NAME_SMI_ERGODIC_OSCILLATOR",  label="SMIEO",  desc="DESC_SMIEO",  // menu="MENU_OSCILLATORS",  helpLink="http://www.motivewave.com/studies/smi_ergodic_oscillator.htm",  overlay=false,  signals=false,  studyOverlay=true)public class SmiErgodicOscillator extends Study{  enum Values { SMIEO, SMI, SIGNAL, CHANGE, ABS_CHANGE, TEMP_ABS_CHANGE, TEMP_CHANGE };  @Override  public void initialize(Defaults defaults)  {    var sd=createSD();    var tab=sd.addTab(get("TAB_GENERAL"));    var inputs=tab.addGroup(get("INPUTS"));    inputs.addRow(new InputDescriptor(Inputs.INPUT, get("INPUT"), Enums.BarInput.CLOSE));    inputs.addRow(new MAMethodDescriptor(Inputs.METHOD, get("METHOD"), Enums.MAMethod.EMA));    inputs.addRow(new IntegerDescriptor(Inputs.PERIOD, get("LONG_PERIOD"), 20, 1, 999, 1));    inputs.addRow(new IntegerDescriptor(Inputs.PERIOD2, get("SHORT_PERIOD"), 5, 1, 999, 1));    inputs.addRow(new IntegerDescriptor(Inputs.PERIOD3, get("SIGNAL_PERIOD"), 5, 1, 999, 1));    tab=sd.addTab(get("TAB_DISPLAY"));    var settings=tab.addGroup(get("COLORS"));    settings.addRow(new ColorDescriptor(Inputs.UP_COLOR, get("LBL_UP_COLOR"), defaults.getGreen()));    settings.addRow(new ColorDescriptor(Inputs.DOWN_COLOR, get("LBL_DOWN_COLOR"), defaults.getRed()));    var indicators=tab.addGroup(get("INDICATOR"));    indicators.addRow(new IndicatorDescriptor(Inputs.IND, get("IND"), defaults.getLineColor(), null, false, true, true));    var guides=tab.addGroup(get("GUIDE"));    var mg=new GuideDescriptor(Inputs.MIDDLE_GUIDE, get("MIDDLE_GUIDE"), 0, -999.1, 999.1, .1, true);    mg.setDash(new float[] { 3, 3 });    guides.addRow(mg);    // Quick Settings (Tool Bar and Popup Editor)    sd.addQuickSettings(Inputs.INPUT, Inputs.METHOD);    sd.addQuickSettings(new SliderDescriptor(Inputs.PERIOD, get("LONG_PERIOD"), 20, 1, 9999, true, () -> Enums.Icon.SINE_WAVE.get()));    sd.addQuickSettings(new SliderDescriptor(Inputs.PERIOD2, get("SHORT_PERIOD"), 5, 1, 9999, true, () -> Enums.Icon.SINE_WAVE.get()));    sd.addQuickSettings(new SliderDescriptor(Inputs.PERIOD3, get("SIGNAL_PERIOD"), 5, 1, 9999, true, () -> Enums.Icon.SINE_WAVE.get()));    sd.addQuickSettings(Inputs.UP_COLOR, Inputs.DOWN_COLOR);    var desc=createRD();    desc.setLabelSettings(Inputs.INPUT, Inputs.METHOD, Inputs.PERIOD, Inputs.PERIOD2, Inputs.PERIOD3);    desc.exportValue(new ValueDescriptor(Values.SMIEO, get("LBL_SMIEO"), new String[] { Inputs.INPUT, Inputs.METHOD, Inputs.PERIOD, Inputs.PERIOD2, Inputs.PERIOD3 }));    desc.declareBars(Values.SMIEO);    desc.declareIndicator(Values.SMIEO, Inputs.IND);    desc.setRangeKeys(Values.SMIEO);  }  @Override  public void onLoad(Defaults defaults)  {    int p1=getSettings().getInteger(Inputs.PERIOD);    int p2=getSettings().getInteger(Inputs.PERIOD2);    int p3=getSettings().getInteger(Inputs.PERIOD3);    setMinBars(p1 + p2 + p3 + 1);  }  @Override  protected void calculate(int index, DataContext ctx)  {    if (index < 1) return;    int fastPeriod=getSettings().getInteger(Inputs.PERIOD);    int slowPeriod=getSettings().getInteger(Inputs.PERIOD2);    int sigPeriod=getSettings().getInteger(Inputs.PERIOD3);    var method=getSettings().getMAMethod(Inputs.METHOD, Enums.MAMethod.EMA);    Object key=getSettings().getInput(Inputs.INPUT, Enums.BarInput.CLOSE);    var series=ctx.getDataSeries();    double close=series.getDouble(index, key, 0); // close unless changed by user    double prevClose=series.getDouble(index - 1, key, 0);    double change=close - prevClose;    double absChange=Math.abs(change);    series.setDouble(index, Values.CHANGE, change);    series.setDouble(index, Values.ABS_CHANGE, absChange);    if (index < fastPeriod + 1) return;    Double tempChange=series.ma(method, index, fastPeriod, Values.CHANGE);    series.setDouble(index, Values.TEMP_CHANGE, tempChange);    Double tempAbs=series.ma(method, index, fastPeriod, Values.ABS_CHANGE);    series.setDouble(index, Values.TEMP_ABS_CHANGE, tempAbs);    if (index < fastPeriod + slowPeriod + 1) return;    tempChange=series.ma(method, index, slowPeriod, Values.TEMP_CHANGE);    tempAbs=series.ma(method, index, slowPeriod, Values.TEMP_ABS_CHANGE);    if (tempChange == null || tempAbs == null) return;    if (tempAbs == 0) return;    Double smi=tempChange / tempAbs;    series.setDouble(index, Values.SMI, smi);    if (index < fastPeriod + slowPeriod + sigPeriod + 1) return;    Double signal=series.ma(method, index, sigPeriod, Values.SMI);    if (signal == null) return;    Double smieo=smi - signal;    series.setDouble(index, Values.SMIEO, smieo);    var midGuide=getSettings().getGuide(Inputs.MIDDLE_GUIDE);    double midG=midGuide.getValue();    Color upC=getSettings().getColor(Inputs.UP_COLOR);    Color dnC=getSettings().getColor(Inputs.DOWN_COLOR);    if (smieo > midG) series.setBarColor(index, Values.SMIEO, upC);    else series.setBarColor(index, Values.SMIEO, dnC);    series.setComplete(index);  }}